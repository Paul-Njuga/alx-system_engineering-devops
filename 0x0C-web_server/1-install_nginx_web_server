#!/usr/bin/env bash
# Install nginx web server

apt-get update
apt-get -y install nginx
# Allow nginx access through firewall
ufw allow 'Nginx HTTP'
# Edit default ubuntu html page
service nginx start
echo 'Hello World!' > /var/www/html/index.nginx-debian.html
